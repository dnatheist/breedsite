<artifact artifact_id="redpoll-simple-script" artifact_version_id="1" title="script.js" contenttype="text/javascript">
document.getElementById('donateButton').addEventListener('click', function() {
    alert('Redirecting to donation page... Support Red Poll today!');
    window.open('https://redpoll.au/Donate', '_blank');
});
</artifact>
<p>To host this on GitHub Pages:</p>
<ol>
<li>Create a new repository (e.g., <code>redpoll-simple</code>).</li>
<li>Add these files (<code>index.html</code>, <code>styles.css</code>, <code>script.js</code>) to the root.</li>
<li>In repo settings > Pages, select the <code>main</code> branch and root folder.</li>
<li>Push changes—your site will be live at <code>https://&#x3C;username>.github.io/redpoll-simple</code>.</li>
</ol>
<p>This is much simpler than the original (single-page, no forms or dynamic content), but it retains the core messaging and structure. Replace the placeholder image with a real one (upload to your repo). If you'd like additions like more images, a contact form (via external service), or tweaks, just say!</p></artifact>2sExpert
